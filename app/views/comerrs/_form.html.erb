<%= form_for(@comerr) do |f| %>
  <% if @comerr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comerr.errors.count, "error") %> prohibited this comerr from being saved:</h2>

      <ul>
      <% @comerr.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Error Message" %><br>
    <%= f.text_field :message %>
  </div>
  <div class="field">
    <%= f.label "Description of Error" %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label "Error Resolution"%><br>
    <%= f.text_area :resolution %>
  </div>
  <div class="field">
    <%= f.label "Upload Screen Shot of error" %><br>
    <%= f.file_field :image %>
  </div>

  <br>

  <div class="actions">
    <%= f.submit "Add Error"%>
  </div>

  <br>
  
<% end %>
